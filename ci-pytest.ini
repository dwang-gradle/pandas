[pytest]
# Minimal, isolated pytest config for the Develocity CI smoke test.
# Using `pytest -c ci-pytest.ini` prevents pytest from loading pandas'
# own pyproject.toml config (whose filterwarnings import pandas, which is
# not installed in the lightweight CI venv).
